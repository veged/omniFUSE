## `of gen-config`

Prints a sample TOML configuration to stdout. The configuration is
intended to live at `~/.config/omnifuse/config.toml` and lets a human
define named backends and mounts.

This command is for human bootstrap. The CLI flags shown in the other
sections are the canonical interface for scripted / agent use.
