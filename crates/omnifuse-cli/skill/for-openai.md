---

## For OpenAI Agents SDK

Expose the mountpoint to your shell tool by setting its working
directory to the mountpoint. Any agent run that can execute `bash`
against the mountpoint will work without code changes — the mount
looks like a normal directory to every Unix tool.
